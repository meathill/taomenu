# 可选：复制为 .env.production（勿提交）。
# 默认部署脚本会从 wrangler.jsonc 的 vars 注入；此处可覆盖（优先级更高）。
# URL 不要尾斜杠。

NEXT_PUBLIC_WEBSITE_URL=https://taomenu-website.meathill.workers.dev
NEXT_PUBLIC_APP_URL=https://taomenu-app.meathill.workers.dev
